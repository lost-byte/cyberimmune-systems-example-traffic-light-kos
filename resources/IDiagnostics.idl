/* Package with an "IMode" interface declaration. */

package traffic_light.IDiagnostics

const UInt32 MaxLogMessageSize = 256;

typedef sequence<UInt8, MaxLogMessageSize> MsgString;

interface {
    /* IMode interface method */
    DMessage(in UInt32 code, in MsgString msg);
}
